// Solving 19th Exercise

const numbers = [10, 20, 30, 40, 50];
console.log(numbers.some((num) => num > 40));
console.log(numbers.every((num) => num > 5));
console.log(numbers.every((num) => num % 2 == 0));
